name: Docker

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  check:
    runs-on: ubuntu-18.04
    steps:
    - uses: actions/checkout@v2
    - name: test docker build
      run: docker build .